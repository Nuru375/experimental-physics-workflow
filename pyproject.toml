[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "experimental_physics_workflow"
version = "0.1.0"
description = "Quick access tools for physics"
dependencies = [
    "numpy",
    "scipy",
    "matplotlib"
]